services:
  bot:
    build: .
    container_name: detector-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./bot.log:/app/bot.log
    environment:
      - PYTHONUNBUFFERED=1
